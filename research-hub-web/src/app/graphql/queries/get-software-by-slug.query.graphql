query GetSoftwareBySlug($slug: String) {
      softwareCollection(where: {slug: $slug}) {
        items {
          title
          slug
          ssoProtected
          sys {
            id
          }
        }
      }
    }