query AllSubHubChildPages ($slug: String) {
  subHubCollection (where:{slug: $slug}) {
    items {
      sys {
        id
      }
      slug
      title
      viewType
      summary
      bodyText {
        json
      }
      ssoProtected
      searchable
    }
  }
}