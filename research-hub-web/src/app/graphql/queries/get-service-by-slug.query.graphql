query GetServiceBySlug($slug: String) {
    serviceCollection(where: {slug: $slug}) {
        items {
            ssoProtected
            title
            sys {
                id
            }
        }
    }
}