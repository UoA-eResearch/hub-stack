query GetSubHubBySlug($slug: String) {
    subHubCollection(where: {slug: $slug}) {
        items {
            ssoProtected
            title
            sys {
                id
            }
        }
    }
}