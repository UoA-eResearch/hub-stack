query GetEventBySlug($slug: String) {
    eventCollection(where: {slug: $slug}) {
        items {
            ssoProtected
            title
            sys {
                id
            }
        }
    }
}