import { ContentTypeDisplayNamePipe } from './content-type-display-name.pipe';

describe('ContentTypeDisplayNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ContentTypeDisplayNamePipe();
    expect(pipe).toBeTruthy();
  });
});
